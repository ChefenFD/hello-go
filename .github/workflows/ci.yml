name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test og lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5 # ← v5 (Node.js 24)

      - name: Sæt Go op
        uses: actions/setup-go@v6 # ← v6 (Node.js 24)
        with:
          go-version: "1.26"
          cache: true
          cache-dependency-path: go.sum # ← fix til "go.sum not found"

      - name: Download afhængigheder
        run: go mod download

      - name: Byg
        run: go build ./...

      - name: Kør tests
        run: go test -race -coverprofile=coverage.out ./...

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: coverage.out
        continue-on-error: true

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12

  security:
    name: Sikkerhedsscanning
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5 # ← v5 (Node.js 24)

      - name: Sæt Go op
        uses: actions/setup-go@v6 # ← v6 (Node.js 24)
        with:
          go-version: "1.26"
          cache: true
          cache-dependency-path: go.sum # ← fix til "go.sum not found"

      - name: Govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...
